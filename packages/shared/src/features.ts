/**
 * Temporary application-wide feature switches.
 *
 * FBR digital invoicing is enabled by default. Seller settings and token
 * storage still control whether invoices can be submitted successfully.
 */
export const FBR_INTEGRATION_ENABLED: boolean = true;
