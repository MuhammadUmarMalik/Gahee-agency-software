import{describe,expect,it}from"vitest";import{createSupplierInputSchema,supplierPaymentInputSchema}from"@oil-agency/shared";import{supplierAging,supplierPayableMinor}from"./supplier-ledger.service.js";describe("supplier validation",()=>{it("accepts supplier opening payable details",()=>expect(createSupplierInputSchema.safeParse({name:"National Oils",businessName:"National Oils Agency",phone:"03001234567",whatsapp:"03001234567",address:"Lahore",taxIdentifier:"NTN-1",openingBalance:"5000.00",isActive:true}).success).toBe(true));it("validates supplier payment methods",()=>expect(supplierPaymentInputSchema.safeParse({amount:"100.00",method:"BANK_TRANSFER",paidAt:"2026-07-17"}).success).toBe(true))});describe("supplier payable ledger",()=>{const entries=[{creditMinor:10000,debitMinor:0,dueDate:new Date("2026-07-01"),occurredAt:new Date("2026-06-01")},{creditMinor:0,debitMinor:4000,dueDate:null,occurredAt:new Date("2026-06-15")}];it("credit purchases increase and payments reduce payable",()=>expect(supplierPayableMinor(entries)).toBe(6000));it("uses FIFO payments for overdue payable",()=>expect(supplierAging(entries,new Date("2026-07-17")).overdueMinor).toBe(6000));it("cancellation is an inverse reversal entry",()=>expect(supplierPayableMinor([...entries,{creditMinor:0,debitMinor:6000}])).toBe(0));it("supplier advances offset later purchases",()=>{const rows=[{creditMinor:0,debitMinor:2000,dueDate:null,occurredAt:new Date("2026-01-01")},{creditMinor:5000,debitMinor:0,dueDate:new Date("2026-02-01"),occurredAt:new Date("2026-01-10")}];expect(supplierAging(rows,new Date("2026-03-01")).overdueMinor).toBe(3000)})});
