const escape = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]!);
const shell = (title: string, body: string) => `<!doctype html><html><head><meta charset="utf-8"><style>@page{margin:10mm}body{font:12px Arial;color:#111}h1,h2{text-align:center}table{width:100%;border-collapse:collapse}th,td{padding:7px;border-bottom:1px solid #ddd;text-align:left}.right{text-align:right}.summary{margin:16px 0 16px auto;width:280px}.row{display:flex;justify-content:space-between;padding:4px}.total{font-size:15px;font-weight:bold}</style></head><body><h2>Oil &amp; Ghee Agency</h2><h1>${escape(title)}</h1>${body}</body></html>`;
export async function printCustomerStatement(statement: { customer: { name: string; businessName: string | null; code: string }; previousBalance: string; currentBalance: string; overdue: string; entries: Array<{ id: string; occurredAt: string; entryType: string; notes: string | null; debit: string; credit: string; currentBalance: string }> }) { const rows = statement.entries.map((entry) => `<tr><td>${escape(new Date(entry.occurredAt).toLocaleDateString())}</td><td>${escape(entry.entryType)}</td><td>${escape(entry.notes ?? "")}</td><td class="right">${escape(entry.debit)}</td><td class="right">${escape(entry.credit)}</td><td class="right">${escape(entry.currentBalance)}</td></tr>`).join(""); const html = shell(`Khata statement — ${statement.customer.name}`, `<p>${escape(statement.customer.businessName ?? "")} · ${escape(statement.customer.code)}</p><table><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Debit</th><th>Credit</th><th>Balance</th></tr></thead><tbody>${rows}</tbody></table><div class="summary"><div class="row"><span>Previous balance</span><b>PKR ${escape(statement.previousBalance)}</b></div><div class="row total"><span>Current balance</span><span>PKR ${escape(statement.currentBalance)}</span></div><div class="row"><span>Overdue</span><span>PKR ${escape(statement.overdue)}</span></div></div>`); if (!window.agencyDesktop?.printPdf) throw new Error("Printing is only available in the desktop app."); return window.agencyDesktop.printPdf(html, `statement-${statement.customer.code}`); }
export async function printPaymentReceipt(payment: { receiptNumber: string; paidAt: string; amount: string; method: string; reference: string | null; customer: { name: string; businessName: string | null } }, reprint = false) { const html = shell("Payment receipt", `${reprint ? '<div style="position:fixed;inset:40% 0;text-align:center;transform:rotate(-35deg);font-size:50px;color:#ddd">REPRINT</div>' : ""}<p><b>Receipt:</b> ${escape(payment.receiptNumber)}</p><p><b>Customer:</b> ${escape(payment.customer.name)} ${escape(payment.customer.businessName ?? "")}</p><p><b>Date:</b> ${escape(new Date(payment.paidAt).toLocaleString())}</p><p><b>Method:</b> ${escape(payment.method)}</p><p><b>Reference:</b> ${escape(payment.reference ?? "—")}</p><div class="row total"><span>Amount received</span><span>PKR ${escape(payment.amount)}</span></div><p style="text-align:center;margin-top:30px">Thank you</p>`); if (!window.agencyDesktop?.printThermal) throw new Error("Printing is only available in the desktop app."); return window.agencyDesktop.printThermal(html); }
