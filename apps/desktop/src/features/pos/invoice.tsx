import { Button } from "@/components/ui/button";
export interface SaleInvoice {
  id: string; invoiceNumber: string; soldAt: string; subtotal: string; discount: string; tax: string; total: string; paid: string; due: string; previousBalance: string; currentBalance: string; notes: string | null;
  business?: { name: string; nameUrdu: string; address: string; addressUrdu: string; phone: string; logoDataUrl: string | null; thankYou: string; thankYouUrdu: string };
  customer: { name: string; businessName?: string | null; phone: string | null; address?: string | null } | null; createdBy: { displayName: string };
  items: Array<{ id: string; packingName: string; unitsPerPack: number; packQuantity: number; baseQuantity: number; quantityBase: number; unitPrice: string; discount: string; tax: string; lineTotal: string; product: { name: string; sku: string; baseUnit: { symbol: string } }; batch: { batchNumber: string; expiryDate: string | null } | null }>;
  payments: Array<{ receiptNumber: string; method: string; amount: string; reference: string | null }>;
}
const fallback = { name: "Oil & Ghee Agency", nameUrdu: "آئل اینڈ گھی ایجنسی", address: "Main Market, Pakistan", addressUrdu: "مین مارکیٹ، پاکستان", phone: "", logoDataUrl: null, thankYou: "Thank you for your business", thankYouUrdu: "آپ کی خریداری کا شکریہ" };
export function Invoice({ sale, reprint = false }: { sale: SaleInvoice; reprint?: boolean }) { const shop = sale.business ?? fallback; return <div className="relative mx-auto bg-white p-6 text-sm text-slate-900">{reprint && <div className="pointer-events-none absolute inset-0 grid place-items-center overflow-hidden text-7xl font-black text-slate-100 -rotate-45">REPRINT</div>}<div className="relative"><header className="flex items-center justify-center gap-4 border-b pb-4 text-center"><Logo src={shop.logoDataUrl}/><div><h2 className="m-0 text-xl font-bold">{shop.name}</h2><p dir="rtl" lang="ur" className="m-0 mt-1 text-base font-semibold">{shop.nameUrdu}</p><p className="m-0 mt-1 text-xs text-muted-foreground">{shop.address} · {shop.phone || "Phone: —"}</p></div></header><div className="grid grid-cols-2 gap-x-6 gap-y-1 border-b py-3 text-xs"><span><b>Invoice:</b> {sale.invoiceNumber}</span><span><b>Date:</b> {new Date(sale.soldAt).toLocaleString("en-PK", { timeZone: "Asia/Karachi" })}</span><span><b>Cashier:</b> {sale.createdBy.displayName}</span><span><b>Customer:</b> {sale.customer?.businessName ?? sale.customer?.name ?? "Walk-in customer"}</span></div><div className="overflow-hidden"><table className="w-full table-fixed text-xs"><thead><tr className="border-b bg-slate-50"><th className="w-[27%] p-2 text-left">Item</th><th className="p-2 text-right">Qty</th><th className="p-2 text-left">Unit</th><th className="p-2 text-right">Rate</th><th className="p-2 text-right">Disc.</th><th className="p-2 text-right">Tax</th><th className="p-2 text-right">Total</th></tr></thead><tbody>{sale.items.map((item) => <tr className="border-b" key={item.id}><td className="break-words p-2"><b>{item.product.name}</b><span className="block text-[10px] text-muted-foreground">{item.product.sku}{item.batch ? ` · ${item.batch.batchNumber}` : ""}</span></td><td className="p-2 text-right">{item.quantityBase}</td><td className="p-2">{item.product.baseUnit.symbol}</td><td className="p-2 text-right">{item.unitPrice}</td><td className="p-2 text-right">{item.discount}</td><td className="p-2 text-right">{item.tax}</td><td className="p-2 text-right font-semibold">{item.lineTotal}</td></tr>)}</tbody></table></div><div className="ml-auto mt-4 w-72 space-y-1 text-xs"><Row label="Subtotal" value={sale.subtotal}/><Row label="Total discount" value={`-${sale.discount}`}/><Row label="Tax" value={sale.tax}/><Row label="Grand total" value={sale.total} strong/><Row label="Paid" value={sale.paid}/><Row label="Due" value={sale.due} strong/><div className="border-t pt-1"><Row label="Previous balance" value={sale.previousBalance}/><Row label="Current balance" value={sale.currentBalance} strong/></div></div><div className="mt-4 border-t pt-3 text-xs"><b>Payment method:</b> {sale.payments.length ? sale.payments.map((payment) => `${payment.method.replaceAll("_", " ")} (PKR ${payment.amount})`).join(" · ") : "CREDIT"}</div><footer className="mt-5 text-center text-xs"><p className="m-0 font-semibold">{shop.thankYou}</p><p dir="rtl" lang="ur" className="m-0 mt-1 text-sm">{shop.thankYouUrdu}</p></footer></div></div>; }
function Logo({ src }: { src: string | null }) { return src?.startsWith("data:image/") ? <img src={src} alt="Shop logo" className="h-14 w-14 object-contain"/> : <svg aria-label="Shop logo" className="h-14 w-14 text-emerald-700" viewBox="0 0 64 64"><path fill="currentColor" d="M32 3C26 15 14 27 14 40a18 18 0 0 0 36 0C50 27 38 15 32 3Z"/><path fill="#fff" opacity=".75" d="M25 47c-5-7-1-14 3-20-1 9 2 14 10 18-4 5-9 6-13 2Z"/></svg>; }
function Row({ label, value, strong = false }: { label: string; value: string; strong?: boolean }) { return <div className={`flex justify-between gap-4 ${strong ? "text-sm font-bold" : ""}`}><span>{label}</span><span className="whitespace-nowrap">PKR {value}</span></div>; }
export function InvoiceActions({ onThermal, onA4, onPdf, onClose }: { onThermal(): void; onA4(): void; onPdf(): void; onClose(): void }) { return <div className="sticky bottom-0 flex justify-end gap-2 border-t bg-slate-50 p-4"><Button variant="outline" onClick={onClose}>Close</Button><Button variant="outline" onClick={onPdf}>Save A4 PDF</Button><Button variant="outline" onClick={onThermal}>80mm print</Button><Button onClick={onA4}>Print A4</Button></div>; }
