import{ArrowLeft,Phone}from"lucide-react";import{useEffect,useState}from"react";import{useNavigate}from"react-router-dom";import{apiRequest}from"@/api/client";import{Button}from"@/components/ui/button";import{Card}from"@/components/ui/card";import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from"@/components/ui/table";import{useAuthStore}from"@/stores/auth-store";type Row={id:string;code:string;name:string;businessName:string|null;phone:string|null;whatsapp:string|null;payable:string;overdue:string;earliestDueDate:string|null};export function SupplierOutstandingPage(){const token=useAuthStore(s=>s.token)!;const nav=useNavigate();const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{void apiRequest<{suppliers:Row[]}>("/suppliers/outstanding",{},token).then(r=>setRows(r.suppliers))},[token]);return <section className="p-8"><Button variant="ghost" className="mb-5 -ml-3" onClick={()=>nav("/suppliers")}><ArrowLeft className="mr-2"/>Suppliers</Button><p className="mb-2 text-sm font-semibold text-primary">SUPPLIER OUTSTANDING</p><h1 className="mt-0 text-3xl">Current payable report</h1><Card><Table><TableHeader><TableRow><TableHead>Supplier</TableHead><TableHead>Phone</TableHead><TableHead>WhatsApp</TableHead><TableHead>Earliest due</TableHead><TableHead>Payable</TableHead><TableHead>Overdue</TableHead></TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r.id} className="cursor-pointer" onClick={()=>nav(`/suppliers/${r.id}`)}><TableCell><strong>{r.name}</strong><span className="block text-xs text-slate-500">{r.businessName??r.code}</span></TableCell><TableCell><Phone className="mr-1 inline" size={14}/>{r.phone??"—"}</TableCell><TableCell>{r.whatsapp??"—"}</TableCell><TableCell>{r.earliestDueDate?new Date(r.earliestDueDate).toLocaleDateString():"—"}</TableCell><TableCell className="font-bold">PKR {r.payable}</TableCell><TableCell className="font-bold text-red-700">PKR {r.overdue}</TableCell></TableRow>)}</TableBody></Table></Card></section>}
